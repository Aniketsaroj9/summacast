---
phase: 04-ai-summarization-chapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/models.py, backend/requirements.txt, worker/db.js]
autonomous: true
requirements: []
must_haves:
  truths:
    - "PostgreSQL db schema updates on backend startup"
    - "worker/db.js saveSummaryAndChapters updates media summary and inserts chapters correctly"
  artifacts:
    - path: "backend/models.py"
      provides: "Chapter database model and relationship with Media"
    - path: "worker/db.js"
      provides: "saveSummaryAndChapters database writer function"
---

<objective>
Update database schema models with a summary field on Media and a new Chapter model, and implement database write helpers for the summary and chapters inside the worker database client.

Purpose: Creates the persistent storage tables and worker helpers needed for AI summarization and chapters.
Output: Modifications to backend/models.py, backend/requirements.txt, worker/db.js
</objective>

<threat_model>
## Threat Model Analysis (ASVS Level 1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| SQL Injection in chapter saves | High | Use parameterized batch SQL queries inside `saveSummaryAndChapters` in `worker/db.js`. Do not use string concat for raw queries. |
| DB connection leaks | Medium | Ensure client connections are properly released in try-finally blocks inside `worker/db.js`. |
</threat_model>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/models.py
@worker/db.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update backend database models and python dependencies</name>
  <files>backend/models.py, backend/requirements.txt</files>
  <read_first>backend/models.py, backend/requirements.txt</read_first>
  <action>
    1. Add `httpx` to `backend/requirements.txt`.
    2. Modify `backend/models.py` to:
       - Add `summary = Column(String, nullable=True)` to the `Media` class.
       - Add relationship `chapters = relationship("Chapter", back_populates="media", cascade="all, delete-orphan")` to `Media`.
       - Implement `Chapter` database model:
         ```python
         class Chapter(Base):
             __tablename__ = "chapters"
             id = Column(Integer, primary_key=True, index=True)
             media_id = Column(Integer, ForeignKey("media_files.id", ondelete="CASCADE"), nullable=False, index=True)
             start_time = Column(Float, nullable=False)
             title = Column(String, nullable=False)
             summary = Column(String, nullable=True)

             media = relationship("Media", back_populates="chapters")
         ```
  </action>
  <verify>python -c "from backend.models import Chapter, Media; print(Chapter.__tablename__)"</verify>
  <acceptance_criteria>
    - `backend/requirements.txt` contains `httpx`.
    - `backend/models.py` contains `Chapter` class mapping to `chapters` table.
    - `Media` contains `summary` column and `chapters` relationship.
    - Test import completes successfully.
  </acceptance_criteria>
  <done>Backend database models and python dependencies are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Implement saveSummaryAndChapters in worker database module</name>
  <files>worker/db.js</files>
  <read_first>worker/db.js</read_first>
  <action>
    Modify `worker/db.js` to:
    1. Implement and export `saveSummaryAndChapters(mediaId, summary, chapters)`:
       - It should connect to the PostgreSQL database via pool.
       - Use a single database transaction (`BEGIN`, `COMMIT`, `ROLLBACK`).
       - UPDATE the `media_files` table setting the `summary` column for the given `mediaId`.
       - If `chapters` array is provided and not empty, perform a bulk INSERT into `chapters` table (`media_id`, `start_time`, `title`, `summary`) using parameterized SQL queries safely.
       - Properly release the database client in a `finally` block.
  </action>
  <verify>node -e "const db = require('./worker/db'); if (typeof db.saveSummaryAndChapters !== 'function') throw new Error('saveSummaryAndChapters not exported');"</verify>
  <acceptance_criteria>
    - `worker/db.js` exports `saveSummaryAndChapters`.
    - `saveSummaryAndChapters` uses parameters (`$1`, `$2`, etc.) and transactions (`BEGIN`/`COMMIT`/`ROLLBACK`).
    - The client connection is released.
  </acceptance_criteria>
  <done>Worker database helper for summary and chapters is implemented.</done>
</task>

</tasks>

<verification>
- Check python backend imports and make sure SQLAlchemy models parse cleanly.
- Verify node worker db helper exposes `saveSummaryAndChapters` function.
</verification>

<success_criteria>
- New PostgreSQL models and tables (`chapters`) are correctly initialized.
- Worker can safely persist summary and chapters to database.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-summarization-chapters/04-01-SUMMARY.md`
</output>
