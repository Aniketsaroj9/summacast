---
phase: 04-ai-summarization-chapters
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [backend/main.py, worker/index.js, worker/summarize.js, worker/.env]
autonomous: true
requirements: []
must_haves:
  truths:
    - "worker/summarize.js generates summary and chapters via Ollama API"
    - "FastAPI GET /api/media/{id}/summary returns the summary and chapters"
    - "FastAPI POST /api/media/{id}/qa answers questions about the media file transcript"
  artifacts:
    - path: "worker/summarize.js"
      provides: "Ollama client wrapper for summarization and chapter generation"
    - path: "backend/main.py"
      provides: "FastAPI endpoints for Q&A and summary/chapters retrieval"
---

<objective>
Implement the Ollama API summarization and chapter generation logic in Node.js, update the background worker process to chain transcription with summarization, and add FastAPI endpoints to retrieve summary details and run interactive Q&A.

Purpose: Completes Phase 4 AI capabilities using local Ollama.
Output: worker/summarize.js, updates to worker/index.js, worker/.env, backend/main.py
</objective>

<threat_model>
## Threat Model Analysis (ASVS Level 1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Prompt Injection in Q&A API | Medium | Structure the LLM prompt carefully by separating context from user question using distinct separators, and instructing the model to stick strictly to the context. |
| Host/Service DOS through massive context sizes | Medium | Enforce a maximum character limit on transcripts sent to Ollama, and restrict the `num_ctx` window size to a safe maximum (e.g. 16384). |
</threat_model>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-summarization-chapters/04-01-PLAN.md
@backend/main.py
@worker/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add default Ollama configuration to worker environment file</name>
  <files>worker/.env</files>
  <read_first>worker/.env</read_first>
  <action>
    Update `worker/.env` to include:
    - `OLLAMA_HOST=http://localhost:11434`
    - `OLLAMA_MODEL=qwen3:8b`
  </action>
  <verify>powershell -Command "Select-String -Path worker/.env -Pattern 'OLLAMA_MODEL'"</verify>
  <acceptance_criteria>
    - `worker/.env` contains `OLLAMA_HOST` and `OLLAMA_MODEL`.
  </acceptance_criteria>
  <done>Ollama environment variables configured in worker environment.</done>
</task>

<task type="auto">
  <name>Task 2: Implement summarize and chapters generator module</name>
  <files>worker/summarize.js</files>
  <read_first>worker/db.js</read_first>
  <action>
    Create `worker/summarize.js` implementing:
    1. `generateSummaryAndChapters(segments)`:
       - Uses `global.fetch` to query local Ollama HTTP endpoints.
       - Concatenates the segments text to build a full transcript.
       - Sends a prompt to `/api/generate` to generate a summary under 3-4 paragraphs.
       - Formats segments with float timestamps: e.g. `[0.00 - 10.25] text` and prompts `/api/generate` with `format: "json"` to return a JSON array of chapters with: `start_time` (Float), `title` (String), `summary` (String).
       - Cleans up and parses the JSON response. If parsing fails, fall back to a programmatic default segment division to prevent crash.
  </action>
  <verify>node -e "require('./worker/summarize')"</verify>
  <acceptance_criteria>
    - `worker/summarize.js` exists and exports `generateSummaryAndChapters`.
    - It handles fetch calls asynchronously using async/await.
    - JSON format parsing for chapters is wrapped in try-catch to avoid crashes.
  </acceptance_criteria>
  <done>Summarization and chapter generation module is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Update worker loop to chain summarization</name>
  <files>worker/index.js</files>
  <read_first>worker/index.js</read_first>
  <action>
    Modify `worker/index.js` to:
    1. Import `generateSummaryAndChapters` from `./summarize` and `saveSummaryAndChapters` from `./db`.
    2. In the job processing loop:
       - After `saveTranscriptSegments(mediaId, segments)` succeeds:
         - Call `updateMediaStatus(mediaId, 'SUMMARIZING')`.
         - Generate summary and chapters: `const { summary, chapters } = await generateSummaryAndChapters(segments)`.
         - Save summary and chapters: `await saveSummaryAndChapters(mediaId, summary, chapters)`.
         - Update status to `'COMPLETED'`.
  </action>
  <verify>node -e "require('./worker/index')"</verify>
  <acceptance_criteria>
    - `worker/index.js` includes calls to `updateMediaStatus(mediaId, 'SUMMARIZING')`, `generateSummaryAndChapters`, and `saveSummaryAndChapters`.
    - Status is updated to `'COMPLETED'` on success.
  </acceptance_criteria>
  <done>Worker loop updated to run summarization and chapters generation.</done>
</task>

<task type="auto">
  <name>Task 4: Expose summary retrieval and Q&A endpoints in FastAPI</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py</read_first>
  <action>
    Update `backend/main.py` to:
    1. Add `OLLAMA_HOST = os.getenv("OLLAMA_HOST", "http://localhost:11434")` and `OLLAMA_MODEL = os.getenv("OLLAMA_MODEL", "qwen3:8b")`.
    2. Add `GET /api/media/{id}/summary` endpoint:
       - Queries database for `Media` file.
       - If not found, raises 404.
       - Returns `summary`, media file details, and array of chapters associated with this media (ordered by `start_time` asc).
    3. Add `POST /api/media/{id}/qa` endpoint taking a body schema `{"question": str}`:
       - Queries database for `Media` file and its `TranscriptSegment`s.
       - If not found, raises 404.
       - Combines transcript segment text into context.
       - Sends context + question to local Ollama generate API (using `httpx.post`).
       - Returns `{"answer": answer_text}`.
  </action>
  <verify>python -c "from backend.main import app; print(app.routes)"</verify>
  <acceptance_criteria>
    - `GET /api/media/{id}/summary` endpoint is defined.
    - `POST /api/media/{id}/qa` endpoint is defined and handles async httpx call to Ollama.
  </acceptance_criteria>
  <done>FastAPI retrieval and Q&A endpoints are exposed.</done>
</task>

</tasks>

<verification>
- Run FastAPI app and verify routes.
- Mock Ollama response or run with local Ollama to ensure endpoints and worker execute successfully.
</verification>

<success_criteria>
- End-to-end processing changes media status from `UPLOADED` -> `PROCESSING` -> `SUMMARIZING` -> `COMPLETED`.
- Transcripts are successfully fed to Ollama to save summary and chapters to database.
- Q&A endpoint retrieves transcript and returns AI answer.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-summarization-chapters/04-02-SUMMARY.md`
</output>
