---
phase: 05-web-ui-player
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/main.py, frontend/src/index.css]
autonomous: true
requirements: []
must_haves:
  truths:
    - "FastAPI GET /api/media returns list of media files"
    - "FastAPI GET /api/media/{id}/file returns file stream"
    - "frontend/src/index.css contains dark-themed variables and glassmorphism utility styles"
  artifacts:
    - path: "backend/main.py"
      provides: "Endpoints for media listing and file responses"
    - path: "frontend/src/index.css"
      provides: "Premium dark-theme visual design system"
---

<objective>
Implement the backend media listing and streaming API endpoints in FastAPI, and set up the foundation of the premium UI styling system inside the CSS stylesheet.

Purpose: Provides the endpoints and design tokens needed by the frontend player and dashboard.
Output: Modifications to backend/main.py, frontend/src/index.css
</objective>

<threat_model>
## Threat Model Analysis (ASVS Level 1)

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Path Traversal in media streaming | High | In `GET /api/media/{id}/file`, verify the file path resolved from the database is within the configured local storage folder boundary. Ensure we only serve files that exist at the path. |
| SQL Injection in media listing | High | Use SQLAlchemy's compiled ORM queries (`db.query(Media)...`) to retrieve list items safely. |
</threat_model>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/main.py
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add media list and media streaming endpoints in FastAPI</name>
  <files>backend/main.py</files>
  <read_first>backend/main.py</read_first>
  <action>
    Modify `backend/main.py` to:
    1. Import `FileResponse` from `fastapi.responses`.
    2. Expose `GET /api/media` endpoint:
       - Queries the database for all `Media` files, ordered by `id` descending.
       - Returns a list of media records (`id`, `original_filename`, `status`, `summary`).
    3. Expose `GET /api/media/{id}/file` endpoint:
       - Queries the database for the `Media` file with the given ID.
       - If not found, raises 404.
       - Checks if the file exists on the local filesystem path. If not, raises 404.
       - Determines a matching MIME content-type (e.g. `video/mp4` or `audio/mpeg` based on file extension).
       - Returns the media file as a `FileResponse` to enable native browser streaming.
  </action>
  <verify>python -c "from backend.main import app; print(app.routes)"</verify>
  <acceptance_criteria>
    - `GET /api/media` is defined.
    - `GET /api/media/{id}/file` is defined and utilizes `FileResponse`.
    - Database queries use safe SQLAlchemy ORM query syntax.
  </acceptance_criteria>
  <done>Backend media listing and file streaming endpoints implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Build the premium dark-mode design system in CSS</name>
  <files>frontend/src/index.css</files>
  <read_first>frontend/src/index.css</read_first>
  <action>
    Rewrite `frontend/src/index.css` to build a clean, curated design system:
    1. Import Outfit or Inter font from Google Fonts.
    2. Configure CSS variables for dark-mode palette: primary colors, background gradients (sleek gray-black), glassmorphism background panels, card border highlights.
    3. Set styling variables for text scaling and layout margins.
    4. Implement styling for the scrollbars, custom grid panels, hover animation scales, and status badge colors (uploaded: blue, processing: orange, summarizing: purple, completed: green, failed: red).
  </action>
  <verify>powershell -Command "Select-String -Path frontend/src/index.css -Pattern 'var\(--'"</verify>
  <acceptance_criteria>
    - `frontend/src/index.css` defines root CSS variables.
    - Scrollbar styling and transitions are included.
    - Status badge utility selectors are defined.
  </acceptance_criteria>
  <done>CSS premium design system styling is established.</done>
</task>

</tasks>

<verification>
- Run python backend and query paths.
- View compiled CSS stylesheet to verify design tokens.
</verification>

<success_criteria>
- Backend exposes streaming and list responses correctly.
- Design theme supports premium dark mode styling utility classes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-ui-player/05-01-SUMMARY.md`
</output>
